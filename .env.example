# Root-level env example - copy to .env and fill in values.
# Each app has its own .env.example with app-specific variables.

# -- Backend --
NODE_ENV=development
PORT=3001
HOST=0.0.0.0
CORS_ORIGIN=http://localhost:3000

# -- Frontend --
NEXT_PUBLIC_API_URL=http://localhost:3001
